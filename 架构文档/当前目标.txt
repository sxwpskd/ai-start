# 项目当前目标（战略层 · 阶段意图）

更新时间：2026-09-19（现状与差距表随 B7 后端编码完成刷新；目标总览不变）
（前次 2026-09-19：现状与差距表随 B4/B5 验收通过、B6 编码完成刷新；
目标总览不变）
（前次 2026-09-17：按文档体系重构，决策详情与实施顺序已移交 开发文档.txt，
本文档瘦身为"目标 + 现状差距"）
关联文档：架构文档/流程.txt（行为层）、
架构文档/开发文档.txt（执行+验收层 · 决策记录）、
架构文档/当前对项目架构的整理与理解.txt（架构基准）

======================================================================
## 〇、文档体系与可信度（2026-09-17 确立）
======================================================================

| 文件 | 层次 | 回答的问题 | 可信度与更新节奏 |
|---|---|---|---|
| 当前对项目架构的整理与理解.txt | 架构基准层 | 代码库是什么 | 最高；变更最少，里程碑后对照代码校验 |
| 当前目标.txt（本文档） | 战略层 | 本阶段做什么、为什么 | 意图快照，过期最快；阶段切换时重写 |
| 流程.txt | 行为层 | 现在怎么跑、之后该怎么跑 | 【现有】须随代码同步，脱节即失真 |
| 开发文档.txt | 执行+验收层 | 具体怎么做、做到什么算完 | 决策与实施顺序唯一归属；完成后逐条核对 |

分工红线：决策只住 开发文档.txt；目标意图只住本文档；流程对照只住 流程.txt；
长期事实只住架构基准。内容冲突时仲裁顺序：代码 > 开发文档.txt 决策 > 其他文档。

======================================================================
## 一、目标总览
======================================================================

1. RAG 实现：作为一个节点的形式插入 langgraph4j
2. base 模式：生成代码前均为 base 模式，流式输出；前端按"生成代码"按钮前都为
   base 模式；工作流为调用特定工具（将对项目的构思写入特定文件 think_会话id.txt）
   → 将特定文件内容作为 HTML 展示在前端浏览器
3. 特定文件同 RAG 实现：作为一个节点的形式插入 langgraph4j
4. 代码生成走工作流：将演示链路转正，点击"生成代码"后由 LangGraph4j 编排生成

目标 1~4 已通过"双通道"设计融合（拍板依据与细节见 开发文档.txt 决策记录）：
节点形态用于工作流通道；直连通道下 rag/think 以工具形态调用。
其中 RAG 相关部分整体搁置后置（恢复倾向见 开发文档.txt 决策记录第 9 条）。

======================================================================
## 二、现状与差距
======================================================================

| 能力 | 现状 | 差距 |
|---|---|---|
| RAG | 仅 LoveAppDocumentLoader 读 md；两张图已预留占位插槽（RagNode） | embedding 选型、向量库、检索器（整体搁置，决策记录第 9 条） |
| base 模式 | 直连通道闭环（B4/B5 验收通过）；工作流通道编码完成（B6） | B6 联调验收 |
| think 渲染 | 已实现（B3：md+html 成对写，/static/base_{appId}/ 预览） | — |
| 工作流转正 | 双图均接业务：构思图（B6）+ 生成图（B7：appId/记忆/think 打通、路由兜底、RLock 防重、SSE 进度推送），编译通过 | B6/B7 联调验收；D4 双构建拍板（联调前） |
| 生成触发 | 直连已实现（B5）；工作流通道后端已编码（B7：SSE 端点 + think 预检端点） | 前端接线（F4：EventSource/口令/预检弹窗/断线对账） |
| 前端 | F1/F2/F3 已实现；B6 最小接线完成（工作流构思对话 + 预览刷新） | F4 全套（loading/阶段文案/失败重试）；F5 BASE 期可视化编辑入口放开 |

======================================================================
## 三、评估结论摘要
======================================================================

- 难度：★★★☆☆（由 ★★★★☆ 降级：工作流不流式，流式全由直连现成链路承接）
- 可行性：高（无技术不可行点；主要风险为同步阻塞超时与 embedding 依赖版本兼容）
- 范围：后端新增约 8~14 文件（RagService/双工具/双节点/两图/渲染/触发接口/
  embedding 配置），修改约 5~8 文件（工厂、Facade、AppServiceImpl、Parser/Saver
  BASE 分支、静态资源）；前端独立仓库两按钮与预览刷新
